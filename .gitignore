# OS / editor cruft
.DS_Store
Thumbs.db

# Worker local build state — regenerable, never committed
worker/node_modules/
worker/.wrangler/
worker/dist/
worker/.dev.vars
*.log

# Personal Claude Code settings — not shared with the team
.claude/settings.local.json
