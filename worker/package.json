{
  "name": "lydia-analytics-worker",
  "version": "0.1.0",
  "private": true,
  "description": "TikTok Creator Analytics OAuth + data pipeline (single-user, internal). Cloudflare Worker.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:list": "wrangler kv namespace list"
  },
  "devDependencies": {
    "wrangler": "^4.103.0",
    "typescript": "^5.6.0",
    "@cloudflare/workers-types": "^4.20240909.0"
  }
}
